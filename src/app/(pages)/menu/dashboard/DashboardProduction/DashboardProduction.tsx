import { LoginUser } from "app/interfaces/User";

interface DashboardProduction {
  loginUser: LoginUser;
}

export const DashboardProduction: React.FC<DashboardProduction> = ({
  loginUser,
}) => {
  return <></>;
};
