const jpontConfig = {
  apiBaseUrl: 'https://jpontokronapi.azurewebsites.net/api/',
  theme: {
    primaryColor: '#1565C0', // Azul
    // otras configs específicas para jpont
  },
};

export default jpontConfig;
