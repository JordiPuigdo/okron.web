const kolderConfig = {
  apiBaseUrl: 'https://kolderapi.azurewebsites.net/api/',
  theme: {
    primaryColor: '#2E7D32', // Verde
    // otras configs específicas para kolder
  },
};

export default kolderConfig;
