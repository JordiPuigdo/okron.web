const defaultConfig = {
  apiBaseUrl:
    process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:5254/api/',
  theme: {
    primaryColor: '#9C27B0', // Morado
  },
};

export default defaultConfig;
